# Get a free access key at https://web3forms.com (use the email where you want messages).
# Copy this file to `.env`, paste your key, then restart `npm run dev` (Vite reads env at startup).
# For production, add the same variable in your host’s environment (e.g. Vercel / Netlify).
VITE_WEB3FORMS_ACCESS_KEY=
